<?php $this->layout('layout::default', ['title' => 'Home']) ?>

<h2 class="heading-medium">Delete user</h2>

<form method="post" class="form">

    <?php $this->insert('snippet::form-csrf', ['form' => $form]) ?>

    <?php if ($deletingSelf) { ?>
        <p>You are about to delete your own account. Are you sure you want to continue?</p>
    <?php }else { ?>
        <p>Are you sure you want to delete <?= $user->getName() ?>?</p>
    <?php } ?>

    <div class="form-group">
        <input type="submit" class="button" style="background-color: red;" name="submit" value="Delete">
        <a class="button button-link" href="<?= $this->generateUrl('user', ['id' => $user->getId()]) ?>">Cancel</a>
    </div>

</form>