<?php $this->layout('layout::standard', ['title' => 'Home']) ?>

<p>Logged in as <?= $user->getName(); ?></p>

<h2>Your outstanding claims</h2>

<table>
    <thead>
    <tr>
        <th>Claim number</th>
        <th>Donor name</th>
        <th>Date received</th>
        <th>Date claim modified</th>
        <th></th>
    </tr>
    </thead>
    <tbody>
        <?php if (count($user->getClaims())) { ?>
            <?php foreach ($user->getClaims() as $claim) { ?>
                <tr>
                    <td><?= $claim->getReferenceNumber() ?></td>
                    <td><?= $claim->getDonorName() ?></td>
                    <td><?= $claim->getReceivedDateTime()->format('d/m/Y') ?></td>
                    <td><?= ($claim->getUpdatedDateTime() ?: $claim->getReceivedDateTime())->format('d/m/Y') ?></td>
                    <td>View</td>
                </tr>
            <?php } ?>
        <?php } else { ?>
            <tr>
                <td colspan="5">No claims</td>
            </tr>
        <?php } ?>
    </tbody>
</table>

<h2>All claims</h2>

<table>
    <thead>
        <tr>
            <th>Claim number</th>
            <th>Donor name</th>
            <th>Date received</th>
            <th>Date claim modified</th>
            <th>Status</th>
        </tr>
    </thead>
    <tbody>
        <?php if (count($claims)) { ?>
            <?php foreach ($claims as $claim) { ?>
                <tr>
                    <td><?= $claim->getReferenceNumber() ?></td>
                    <td><?= $claim->getDonorName() ?></td>
                    <td><?= $claim->getReceivedDateTime()->format('d/m/Y') ?></td>
                    <td><?= ($claim->getUpdatedDateTime() ?: $claim->getReceivedDateTime())->format('d/m/Y') ?></td>
                    <td><?= $claim->getStatus() ?></td>
                </tr>
            <?php } ?>
        <?php } else { ?>
            <tr>
                <td colspan="5">No claims</td>
            </tr>
        <?php } ?>
    </tbody>
</table>

<p><a href="/caseworker">Case worker user page</a></p>
<p><a href="/reporting">Reporting user page</a></p>
<p><a href="/refund">Refund manager user page</a></p>
<p><a href="/admin">Admin user page</a></p>

<p><a href="sign-out">Sign out</a></p>
